<mjml>
  <mj-body>
    <mj-section>
      <mj-column>
        {{contact.firstname}}
        {{contact.lastname}}
        {{contact.mail}}
        {{contact.message|nl2br}}
      </mj-column>
    </mj-section>
  </mj-body>
</mjml>
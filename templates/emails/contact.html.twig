<mjml>
  <mj-body>
    <mj-section>
      <mj-column>
        {{contact.prenom}}
        {{contact.nom}}
        {{contact.email}}
        {{contact.message|nl2br}}
      </mj-column>
    </mj-section>
  </mj-body>
</mjml>
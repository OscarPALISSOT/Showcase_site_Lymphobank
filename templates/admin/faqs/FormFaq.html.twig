{{form_start (form)}}
<div class="row">
    <div class="col-4">
        {{form_rest (form)}}  
        <button class="btn btn-primary mt-4">{{button|default('Enregistrer')}}</button>
    </div>
</div>
{{form_end (form)}}